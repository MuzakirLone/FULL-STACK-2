version: '3.8'

services:
  backend:
    build:
      context: ./Experiment_16_Backend
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production

  frontend:
    build:
      context: ./Experiment_16_Frontend
    ports:
      - "8080:80"
    depends_on:
      - backend
