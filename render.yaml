services:
  - type: web
    name: experiment-13-student-api
    env: python
    rootDir: Experiment-13
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    autoDeploy: true